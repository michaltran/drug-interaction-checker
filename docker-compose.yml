version: '3.8'
services:
  app:
    build: .
    container_name: drug-interaction
    restart: always
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      PORT: 5000
      JWT_SECRET: ${JWT_SECRET:-doi_mat_khau_nay_123}
    volumes:
      - db_data:/app/backend/database
volumes:
  db_data:
